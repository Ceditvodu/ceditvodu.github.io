this.addEventListener('message', (e)=>{
	let this.addEventListener('message', (e)=>{
	let sections = e.data;
	let render = `
		<g id="ground"></g>
		<g></g>
		<g></g>
		<g></g>
	`;

	setTimeout(function () {
    this.postMessage(render);
  }, 3000);
	
});sections = e.data;
	let render = `
		<g id="ground"></g>
		<g></g>
		<g></g>
		<g></g>
	`;

	setTimeout(function () {
    this.postMessage(render);
  }, 3000);
	
});
